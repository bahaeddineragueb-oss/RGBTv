# RGBTv الأصلي للأندرويد — دليل التحميل والتثبيت (APK حقيقي، ليس WebView)

## ما هذا؟

تطبيق أندرويد **أصلي 100%** مكتوب بـ Kotlin مع مشغّل **ExoPlayer**:

- إقلاع وتشغيل أسرع بكثير من نسخة WebView، مع تسريع عتادي كامل.
- نفس حساباتك: **Xtream Codes / Stalker / M3U** + دليل برامج **XMLTV**.
- بث مباشر + أفلام + مسلسلات + استئناف المشاهدة + تشغيل الحلقة التالية تلقائياً.
- يعمل باللمس على الهاتف، وبأزرار الريموت على **Android TV** (تنقّل كامل).
- قفل عائلي، قائمتي، بحث، عربي/فرنسي/إنجليزي، ألوان تمييز.

## تحميل الـ APK (مجاناً، من GitHub)

1. افتح صفحة المستودع على GitHub.
2. **الطريقة الأسهل:** تبويب **Releases** ← إصدار **RGBTv 3.0.0 — Android native APK**
   ← حمّل ملف `RGBTv-native-3.0.0.apk`.
3. **أو:** تبويب **Actions** ←‏ **Build Android APK (native)** ← افتح آخر تشغيل ناجح (✅)
   ← قسم **Artifacts** ←‏ **RGBTv-native-apk**.

## التثبيت

1. على الهاتف: افتح الملف ووافق على «التثبيت من مصادر غير معروفة» عند الطلب.
2. على Android TV / الـ Box: انسخ الملف بفلاشة USB أو أرسله عبر «Send Files to TV»
   ثم ثبّته من مدير الملفات.
3. ⚠️ إن كانت نسخة RGBTv القديمة (WebView) مثبّتة عندك، **احذفها أولاً** ثم ثبّت
   الجديدة (التوقيع مختلف ولن يقبل التحديث فوقها).

## بناء نسخة جديدة بنفسك

- من GitHub مباشرة: **Actions** ←‏ **Build Android APK (native)** ←‏ **Run workflow**
  (يبني خلال ~5–10 دقائق ويرفق الـ APK تلقائياً في Release).
- من حاسوبك: راجع `native/README.md`.

## ملاحظات

- أول تشغيل على سيرفر كبير قد يأخذ 10–30 ثانية (تحميل القوائم أول مرة فقط،
  ثم تُحفظ محلياً).
- صيغة البث المباشر من الإعدادات: تلقائي / HLS / TS.
- رمز القفل العائلي يُنشأ من الإعدادات ← القفل العائلي.
