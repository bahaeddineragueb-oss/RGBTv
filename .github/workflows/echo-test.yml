name: Echo test
on:
  push:
    branches: ['arena/01a08076-rgbtv']
    paths: ['.github/workflows/echo-test.yml']
jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - run: echo hello-from-arena-branch
